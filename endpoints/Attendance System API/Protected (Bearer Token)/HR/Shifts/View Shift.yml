info:
  name: View Shift
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}shifts/{{shiftId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

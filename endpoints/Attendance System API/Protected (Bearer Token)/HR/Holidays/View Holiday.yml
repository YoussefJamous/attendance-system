info:
  name: View Holiday
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}holidays/{{holidayId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

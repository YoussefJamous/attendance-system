info:
  name: Download Import Template
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}holidays/import-template"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

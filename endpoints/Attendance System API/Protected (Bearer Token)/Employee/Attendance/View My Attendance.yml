info:
  name: View My Attendance
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}attendance/{{attendanceId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

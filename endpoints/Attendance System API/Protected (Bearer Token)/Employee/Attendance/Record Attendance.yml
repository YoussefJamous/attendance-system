info:
  name: Record Attendance
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}attendance/actions"
  body:
    type: multipart-form
    data:
      - name: image
        type: file
        value: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

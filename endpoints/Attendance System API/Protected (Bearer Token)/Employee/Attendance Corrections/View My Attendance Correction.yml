info:
  name: View My Attendance Correction
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}attendance-corrections/{{attendanceCorrectionId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
